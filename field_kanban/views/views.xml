<odoo>
  <data>
    <record id="view_project_kanban_inherit" model="ir.ui.view">
      <field name="name">project.task.inherit</field>
      <field name="model">project.task</field>
      <field name="inherit_id" ref="project.view_task_kanban"/>
      <field name="arch" type="xml">
        <xpath expr="/kanban/templates/t/div//div[@class='oe_kanban_content']" position="after">
          <!-- <a href="web?debug=1#id=8&amp;model=res.partner"> -->
          <field name="project_id" widget="res_partner_many2one"/>
          <!-- </a> -->
        </xpath>
      </field>
    </record>
    <!-- explicit list view definition -->
<!--
    <record model="ir.ui.view" id="field_kanban.list">
      <field name="name">field_kanban list</field>
      <field name="model">field_kanban.field_kanban</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
-->

    <!-- actions opening views on models -->
<!--
    <record model="ir.actions.act_window" id="field_kanban.action_window">
      <field name="name">field_kanban window</field>
      <field name="res_model">field_kanban.field_kanban</field>
      <field name="view_mode">tree,form</field>
    </record>
-->

    <!-- server action to the one above -->
<!--
    <record model="ir.actions.server" id="field_kanban.action_server">
      <field name="name">field_kanban server</field>
      <field name="model_id" ref="model_field_kanban_field_kanban"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": model._name,
        }
      </field>
    </record>
-->

    <!-- Top menu item -->
<!--
    <menuitem name="field_kanban" id="field_kanban.menu_root"/>
-->
    <!-- menu categories -->
<!--
    <menuitem name="Menu 1" id="field_kanban.menu_1" parent="field_kanban.menu_root"/>
    <menuitem name="Menu 2" id="field_kanban.menu_2" parent="field_kanban.menu_root"/>
-->
    <!-- actions -->
<!--
    <menuitem name="List" id="field_kanban.menu_1_list" parent="field_kanban.menu_1"
              action="field_kanban.action_window"/>
    <menuitem name="Server to list" id="field_kanban" parent="field_kanban.menu_2"
              action="field_kanban.action_server"/>
-->
  </data>
</odoo>