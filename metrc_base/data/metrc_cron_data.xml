<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record model="ir.cron" id="cron_ir_logging_archive">
        <field name="name">Metrc: Logging Archive</field>
        <field name="model_id" ref="base.model_ir_logging"/>
        <field name="state">code</field>
        <field name="code"><![CDATA[
# Keyword args:
# batch_size = 1000 (Number of rows in csv)
# level = ['info', 'error', 'warning'] (log levels to consider for archiving)
# Ex. model._cron_archive_metrc_logs(batch_size=500, level=['info'])
# NOTE: This cron will only archieve and delete metrc account loggings.
model._cron_archive_metrc_logs(level=['info'])
]]></field>
        <field name="interval_number">1</field>
        <field name="interval_type">weeks</field>
        <field name="is_metrc">True</field>
        <field name="active">True</field>
        <field name="numbercall">-1</field>
    </record>
</odoo>